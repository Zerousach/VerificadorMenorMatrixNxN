Archivo .c que una vez compilado se utiliza para comprimir matrices del tipo nxn, 
este verifica el numero menor que existe en una matriz nxn comprimiendola en matrices
mas pequeñas, sin utlizar ordenamiento.
DEFECTOS: En matrices del mayores a 128x128 el programa se rompe, se sospecha que es debido a un overflow
